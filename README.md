# TASK.SAYT

TASK.SAYT is an application that runs with Docker. It can be run with a single command: `git clone + docker compose up`.

## Requirements
- Docker
- Docker Compose

## How to Run

```bash
git clone https://github.com/Murad-237/TASK.SAYT.git
cd TASK.SAYT
docker compose up
